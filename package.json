{
  "name": "covid-stats-ireland-static",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "clean": "rm ./cached.json",
    "dev": "NODE_ENV=development eleventy --serve --port=3020",
    "dev:clean": "npm run clean && npm run dev",
    "build-action": "npx @11ty/eleventy --serve",
    "test": "echo \"Error: no test specified\" && exit 1",
    "screenshot": "node ./utils/screenshot",
    "deploy": "eleventy build && gh-pages -d _site"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "csv-string": "^4.0.1",
    "d3-node": "^2.2.2",
    "fs": "0.0.1-security",
    "gh-pages": "^3.1.0",
    "make-fetch-happen": "^8.0.12",
    "node-fetch": "^2.6.1",
    "puppeteer": "^10.4.0"
  }
}
